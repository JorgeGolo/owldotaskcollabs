# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production build outputs (Next.js)
.next/
out/
dist/

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

# Node.js logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Android Application (Capacitor)
# Directorios de build y caché de Android
android/build/
android/.gradle/
android/.idea/ # Archivos de configuración de Android Studio
android/app/build/
android/app/.cxx/
android/app/.externalNativeBuild/
android/app/src/main/assets/capacitor.settings.json # Generado por Capacitor
android/app/src/main/res/xml/capacitor_preferences.xml # Generado por Capacitor
android/local.properties # Ruta del SDK de Android, siempre ignorar

# Archivos de salida de la aplicación Android
*.apk
*.aab
app-release-signed.apk
app-release-bundle.aab
*.idsig
app-release-unsigned-aligned*

# Keystore y archivos de firma (CRÍTICO: ¡NUNCA SUBIR A GIT!)
android/keystore.properties # Archivo de propiedades de la clave de firma
/android/keystore.properties # Archivo de propiedades de la clave de firma
my-release-key.jks # Tu archivo de keystore si está en la raíz del proyecto
android.keystore # Si usas este nombre genérico para tu keystore
android/my-release-key.jks # Si usas este nombre genérico para tu keystore
/android/my-release-key.jks # Si usas este nombre genérico para tu keystore
android/app/google-services-json

# PWA files (if generated by next-pwa)
public/sw.js
/public/workbox-*.js

# Logs (general)
*.log

# Gradle files (general)
.gradle/
build/
gradle/wrapper/.gradle/ # Caché dentro del wrapper de Gradle

# Temporary files
temp/
tmp/
/tmp

# Other generated files
/public/manifest-checksum.txt
manifest-checksum.txt
/public/routesprecache.json